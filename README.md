# Virtual Cell Workshop website

GitHub Pages build using a normal `assets/` folder.

Upload the following together to the repository root:

- `index.html`
- `assets/`
- `robots.txt`
- `.nojekyll`

Do not upload only `index.html`: the speaker portraits are stored in `assets/`.
